import React from "react";

const SectionHeading = (props) => {
  return (
    <span className="text-[#F2F2F2] text-xs font-semibold">
      {props.heading}
    </span>
  );
};

export default SectionHeading;
